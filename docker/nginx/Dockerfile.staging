FROM nginx:alpine

COPY docker/nginx/staging.conf /etc/nginx/conf.d/default.conf

# Port 80 — Coolify/Traefik default for HTTP services.
# Set Health Check URL to GET /up in the Coolify dashboard.
EXPOSE 80
